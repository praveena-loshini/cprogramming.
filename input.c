#include<stdio.h>
int main(){
    int age;
    char name[30];
    scanf("%d%s",&age,&name);
    printf("%d\n%s",age,name);
}//Write a C program to obtain and display the newly joined student's name and age detail. Input Format: The First line of input consists of a string that corresponds to the name of the student. The second line of input consists of an integer that corresponds to the age of the student. Output Format: The output prints the name and age details of the student. 